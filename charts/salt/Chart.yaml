apiVersion: v2
name: salt
description: Salt master on Kubernetes - configuration management and remote execution at scale.
type: application

# Chart version. Bumped on every change to the chart itself.
version: 0.2.0

# The Salt release the default image provides, and therefore the default tag.
# Must track the version docker-build/ publishes.
appVersion: "3008.2"

home: https://saltproject.io
icon: https://saltproject.io/images/logo.svg
sources:
  - https://github.com/saltstack/salt
keywords:
  - configuration-management
  - remote-execution
  - infrastructure-automation
